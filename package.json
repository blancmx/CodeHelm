{
  "name": "codehelm-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "CodeHelm - Local-first personal project management & orchestration desktop application",
  "scripts": {
    "dev": "pnpm --filter @codehelm/desktop dev",
    "build": "pnpm -r --filter \"./packages/*\" run build && pnpm --filter @codehelm/desktop exec vite build",
    "package": "pnpm package:unsigned",
    "package:unsigned": "pnpm build && pnpm --filter @codehelm/desktop exec electron-builder && pnpm verify:release && node apps/desktop/scripts/finalize-unsigned-release.mjs dist-release",
    "package:dir": "pnpm build && pnpm --filter @codehelm/desktop exec electron-builder --dir && pnpm verify:release",
    "verify:release": "node apps/desktop/scripts/verify-release-artifacts.mjs dist-release/win-unpacked/resources",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.7",
    "rimraf": "^6.0.1",
    "@types/node": "^22.13.9"
  }
}
