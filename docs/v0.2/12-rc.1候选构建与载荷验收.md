# 0.2.0-rc.1 候选构建与载荷验收

日期：2026-09-10。候选版本由用户选择；用途为未签名内部验收，尚未执行安装或公开发布。

## 源码与构建

冻结提交：`3baa010879d30ecdd669be718ebcd9029bfebcb9`。独立工作树为 `test-results/v02-rc1-source`，通过 `pnpm.cmd install --frozen-lockfile` 安装依赖，再执行 `pnpm.cmd package:unsigned`。主工作区七个未提交界面文件未纳入构建。

根清单和桌面清单版本更新为 `0.2.0-rc.1`。未签名收尾脚本从桌面清单读取版本，安装器名称、校验清单、提示文件和 JSON 元数据不再写死 `0.1.0`。lint 和脚本语法检查通过。

构建输出：`test-results/v02-rc1-source/dist-release`。构建日志：`test-results/v02-rc1-package.log`。第三方声明由生成器重新生成，工作树报告其变化；Git 内容差异为空，换行归一化比较一致。证据清单保留实际 `dirty=true`，不伪称完全干净的 CI 构建。

## 产物与摘要

| 文件 | SHA-256 |
|---|---|
| CodeHelm Setup 0.2.0-rc.1.exe | `A0185F768907172EB4E909DAF98765504C842F97D31A2A92E23A0A8D1CA48A70` |
| CodeHelm Setup 0.2.0-rc.1.exe.blockmap | `43AFBF7CBD5E3C039A8392024C76AFF6318F6748A8F5DFA1C1CAF0ACD255684C` |
| win-unpacked/CodeHelm.exe | `01D940EA2749E42ABDCEBE8D53482506F98DF7095CBA7BA9BDDCE6F89A02B5FB` |
| win-unpacked/resources/app.asar | `1b8cd2f9b4b848baa2cc742a694a2898398efc6e0292ebd523f6fc4e74b6c1d8` |

三项发布清单摘要均重新计算核对一致，ASAR 中 package.json 为 `0.2.0-rc.1`。Windows EXE 的 FileVersion 为 `0.2.0-rc.1`，ProductVersion 为数值 `0.2.0.0`。Authenticode 检查返回 `NotSigned`。安装器大小 112,766,700 字节。

使用缓存的 7-Zip 仅解包安装器至 `test-results/v02-rc1-extracted`，**104 个载荷文件逐一与已测目录包哈希一致**。7-Zip 对 NSIS 尾部数据给出警告，未发生解包错误；日志在 `test-results/v02-rc1-extract.log`。载荷一致性不证明快捷方式、卸载、升级或默认数据目录行为。

## 实际桌面回归

使用独立工作树测试代码，指定 rc.1 目录包及本机 Python、Java、CSC 路径运行 `pnpm.cmd test:e2e:built`。结果 **16 通过、1 跳过、0 flaky**，57.1 秒。跳过的是本轮未启用的十分钟性能项；不能把旧版本标识下的性能结果声称为 rc.1 新测结果。

报告：`test-results/v02-rc1-source/test-results/packaged-e2e-results.json`。日志：`test-results/v02-rc1-e2e.log`。候选证据：`test-results/v02-rc1-source/test-results/rc1-evidence.json`，记录 110 个文件、明确指定的打包报告及 `releaseReady=false`。

已查看真实恢复完成窗口，备份条目版本为 `0.2.0-rc.1`、schema 3；测试使用临时用户数据并完成实际重开与数据验证，没有操作正式管理数据。

## 尚未完成

安装器已准备可供后续隔离验收，未执行安装、升级、卸载或系统回退。最低支持系统、真实慢盘/网络盘、冷启动及最终发布条件仍待确认或实测。整个 V02-006 不标记通过；公开上传需要另行授权。

相关材料：[候选交付说明](./11-v0.2候选交付说明.md)、[安装升级矩阵](./09-Windows隔离安装升级验收清单.md)。
