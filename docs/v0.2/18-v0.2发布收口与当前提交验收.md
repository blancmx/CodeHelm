# v0.2 发布收口与当前提交验收

日期：2026-09-14。版本：`0.2.0-rc.1`。

## 本轮范围

V02-007～011 的本地功能开发已完成，本轮回到 V02-006 的发布收口工作。以已推送提交 `78a1a0d6cfd11613a163bfa3ade17deda2c8c1d2` 为测试基线，重新验证包含全部已纳入 P1 功能的 Windows 构建，而不是沿用早期提交的 CI 结果。

本轮不新增产品功能；验收发现总览性能超标后，修复了退出动画阻塞新结果的问题。不改动既有 `profile-handlers.ts` 工作区状态。隔离 Windows 安装、升级、回退、冷启动及真实慢盘/网络盘验收继续后置；没有执行安装器或公开发布。

## 当前提交的远端验证

[Windows CI 34857218699](https://github.com/blancmx/CodeHelm/actions/runs/34857218699)，分支 `codex/v02-001-environment-diagnostics`，已核对运行的 `headSha` 与上述基线一致。

启动参数：`performance=true`、`java_stability=true`。

首次运行的基础检查、原生测试和开发版桌面回归通过（22 通过、1 跳过、0 flaky）。目录包构建在 Electron 下载阶段收到 HTTP 504，后续打包回归与性能步骤未执行，不能记作通过。首轮证据保存到 `test-results/v02-final-ci-34857218699-attempt1`，失败日志为 `test-results/v02-011-ci-failed.log`。同一提交第二次运行已完成：打包下载成功，最终因千项目总览 P95 超标失败。第二轮报告位于 `test-results/v02-final-ci-34857218699-attempt2`。

| 验收范围 | 本轮状态 |
| --- | --- |
| lint、构建、类型与单元测试、开发版桌面回归 | 第二轮通过；开发版 22 通过、1 跳过、0 flaky |
| 原生文件边界、目录包构建与资源校验 | 第二轮通过 |
| 打包版桌面回归 | 22 通过、1 跳过、0 flaky |
| Java 十次无重试稳定性 | 10 通过、0 跳过、0 flaky |
| 50,000 文件预算与取消 | 通过 |
| 搜索、诊断、热启动矩阵与千项目总览 | 矩阵通过；总览 P95 260.8 ms，超过 200 ms，失败 |
| 10 分钟持续日志、内存与停止响应 | 因前序总览失败未执行，仍待验收 |

本地 V02-011 的 696 项测试、7 项原生测试和开发版/打包版各 22 项桌面回归结果见 [V02-011 验收](./17-V02-011脱敏诊断包验收.md)。这些本地回归跳过的持续日志试验由本轮独立性能步骤执行，不预先记作通过。

## 剩余发布边界

### 总览性能修复与本地对照

`OverviewView.vue` 的 `out-in` 过渡原先等待旧内容的 120 ms 退出动画，随后才挂载新结果。本轮取消退出等待，保留 160 ms 进入反馈，未调整 200 ms 验收目标、样本数量或测试逻辑。

| 同机、同样本的打包应用对照 | 总览 30 次切换 P95 |
| --- | --- |
| 保留的 V02-011 旧候选 | 199.4 ms |
| 本轮修复候选 | 86.1 ms |

CI 旧候选的 260.8 ms 与本地机器不能直接计算性能改善比例；同机对照表明移除退出等待显著降低切换耗时。本地旧候选虽勉强达标，不推翻 CI 的失败记录。

修复候选通过生产构建、lint、类型检查、66 项 ASAR 资源校验及包含千项目总览的性能矩阵。精确搜索 P95 20.9 ms、宽泛搜索 P95 18.7 ms、本地 Node 诊断 P95 5.13 ms；热启动中位数 785.49 ms，不代表冷启动或旧版启动回退比例。

原始报告：`test-results/v02-final-overview-results.json`，样本与已查看的总览截图：`test-results/v02-final-overview-benchmarks`。旧候选对照：`test-results/v02-final-overview-baseline-results.json` 与 `test-results/v02-final-overview-baseline-benchmarks`。

修复已提交推送为 `6d86784d29519a97f2bc6547ca8d0e13227a0191`，远端 SHA 已核验一致。

### 修复提交的 CI 复验

[CI 34860591857](https://github.com/blancmx/CodeHelm/actions/runs/34860591857) 已确认使用 `6d86784`，启用完整性能与 Java 稳定性检查。首轮 696 项测试通过，但下载 Electron 官方 ZIP 时遇到 HTTP 504，桌面回归未启动；失败日志为 `test-results/v02-final-ci-6d86784-failed.log`。

第二轮基础检查、原生测试、候选包构建与资源校验、开发版及打包版回归、Java 稳定性、扫描边界和搜索诊断矩阵通过。总览 P95 为 **210 ms**，仍超过 **200 ms**，因此整个 CI 失败，持续日志步骤仍未执行。不能用本地 86.1 ms 的通过结果替代这个远端失败。

报告保留在 `test-results/v02-final-ci-6d86784-attempt2`，失败日志为 `test-results/v02-final-ci-6d86784-attempt2-failed.log`。还需继续定位总览在 CI 环境中的切换耗时，并在通过后执行持续日志验收。本阶段尚未全部验收通过。

### 2026-09-15 容器复用与布局移动修复

上一节验收记录已提交推送为 `141ceff`，远端 SHA 已核验。随后发现筛选条件拼入网格/列表根 key，使筛选时整个分页容器重新挂载；进入动画还会移动整块结果。本轮改为稳定的视图 key，筛选时保留容器并按项目 ID 更新子项；切换视图只保留 100 ms 淡入，不移动结果布局。数据筛选、分页容量、200 ms 阈值及 30 次采样逻辑均未改动。

本地新候选 `test-results/v02-overview-stable-package` 已通过构建、lint、类型检查及千项目性能矩阵，总览 P95 **104.8 ms**。已查看实际总览截图，筛选计数、列表和分页显示正常。该数值不证明比上一轮本地 86.1 ms 更快，也不替代远端复验。

证据：`test-results/v02-overview-stable-results.json` 与 `test-results/v02-overview-stable-benchmarks`。包含完整 10 分钟持续日志的打包桌面回归 **23 项通过、0 跳过**，耗时 11.6 分钟。报告与样本保留在 `test-results/v02-overview-stable-full-results.json`、`test-results/v02-overview-stable-full-artifacts`。

持续日志实际吞吐 **999.94 行/秒**，输入 P95 **28.3 ms**，停止响应 **733.5 ms**；稳态工作集由 606,396 KiB 到 612,256 KiB，增长 **0.97%**，满足既定预算。本地候选资源校验通过；资产和源码清单见 `test-results/v02-overview-stable-evidence.json`。这些属于本工作站、隔离数据的目录包结果，不替代远端或隔离安装验收。本轮修复继续按本阶段授权提交至既有分支并复验 CI。

### c5ddafa 远端结果与停止耗时缺口

容器复用修复已推送为 `c5ddafaebc9d8c1153b07c3e6fa329b5775d901e`。[CI 34970811779](https://github.com/blancmx/CodeHelm/actions/runs/34970811779) 状态成功，开发版/打包版各 22 通过、1 跳过、0 flaky；Java 10 次、扫描边界、搜索诊断矩阵均通过。总览 P95 **120.8 ms**，总览性能缺口已关闭。

持续日志报告含 **1 项 flaky**：第一次停止耗时 **1031.3 ms**，超过 1000 ms；重试 **846.1 ms** 通过。两次输入 P95 分别为 57.8/66.8 ms，吞吐约 1000 行/秒，内存变化 -30.19%/-35.24%。因此整个阶段不能仅凭绿色 CI 标记为无稳定性缺口。原始报告和两轮样本保存在 `test-results/v02-ci-c5ddafa`，完整日志为 `test-results/v02-ci-c5ddafa-full.log`。

2026-09-16 继续处理停止耗时：正常停止在 ProcessManager 和真正派发信号的 killProcessTree 内连续执行了两次启动时间核验，Windows 每次调用都会启动 PowerShell。移除外层重复核验，保留派发前即时核验、强制终止前核验及身份不匹配时的拒绝行为。新增守卫测试并在真实长运行子进程测试中核对正常路径仅核验一次。首轮新增测试因 beforeEach 返回 mock 被当作清理函数而失败；已修正为不返回值，专项复验通过。最终全量 **74 个文件、698 项测试通过**，lint、构建与类型检查通过。

候选配置显式设置 `retries: 0`，性能失败不会再被同轮自动重试掩盖；没有修改性能阈值或采样逻辑。本地目录包资源校验通过（66 项 ASAR 条目），资产清单包含 104 个文件。

本地完整打包回归 **21 通过、2 失败、0 重试**，不能记为全量通过：项目整理用例的归档选项在点击前消失并超时，失败快照中最小化按钮处于激活状态，窗口干扰原因尚未确认；10 分钟持续日志的停止耗时 **560.8 ms** 达标，但输入 P95 **1997.3 ms** 超过 200 ms，因此该用例失败。吞吐约 **1000.04 行/秒**，内存未增长。报告、失败 trace 与原始样本保存在 `test-results/v02-stop-identity-full-results.json`、`test-results/v02-stop-identity-full-artifacts`；候选与源码哈希见 `test-results/v02-stop-identity-evidence.json`。本地结果不覆盖之前 CI 的失败，后续在独立 Windows CI 上按相同门槛复验。

### 41fdaa4 复验记录

停止路径修复及候选测试零重试配置已推送为 `41fdaa4e2ccfac51efe44713faec4f7aae6cfe2c`，远端 SHA 核验一致。[CI 35097347142](https://github.com/blancmx/CodeHelm/actions/runs/35097347142) 使用该提交，启用完整性能及 Java 稳定性检查，已完成。698 项单元测试、原生边界测试、打包版 22 项回归、Java 10 次、扫描边界及搜索诊断矩阵通过。总览 P95 **147.5 ms**；10 分钟日志 **首次通过、0 重试**，输入 P95 **55 ms**、停止 **711.2 ms**、吞吐 **1000.05 行/秒**、内存变化 **-32.69%**，性能门槛通过。

但开发版托盘生命周期用例有 **1 项 flaky**，其余 21 项通过、1 项持续日志按计划跳过，因此仍不能宣称全部稳定通过。失败位于重启后零活动服务的窗口隐藏状态查询，调用 `app.evaluate` 超时，自动重试通过；打包版对应用例首次通过。原始报告为 `test-results/v02-ci-41fdaa4`，完整日志为 `test-results/v02-ci-41fdaa4-full.log`。

归档用例在独立、零重试复验中通过（7.6 秒），报告为 `test-results/v02-stop-identity-organize-followup-results.json`。本地第二次独立 10 分钟日志试验仍失败：输入 P95 **2008.2 ms**；停止 **483.4 ms**、吞吐 **1000.01 行/秒**、内存增长 **1.57%** 达标。报告与样本为 `test-results/v02-stop-identity-sustained-followup-results.json`、`test-results/v02-stop-identity-sustained-followup-artifacts`。本次不是自动重试，前一轮失败仍完整保留。

随后使用隔离数据进行窗口状态探测，记录到 `minimized=true`、`focused=false`、`visible=false`，同时双帧回调约 1000 ms；探测文件为 `test-results/v02-stop-window-probe.json`。这支持本机窗口状态干扰的判断，但不能回溯证明两轮长测的全部慢样本均由此导致，也不能把原失败改记为通过。没有为最小化状态放宽前台 200 ms 门槛。

### 托盘验收观测修复

托盘重启断言改为先注册真实 `hide` 事件，在主进程中记录窗口 ID 和不可见状态，再通过既有 `second-instance` 路径恢复窗口，恢复后核对记录与当前窗口 ID。这样避免在没有运行服务的隐藏窗口上等待调试通道往返；没有删除隐藏、恢复、服务保留、退出或配置持久化断言，也没有更改产品代码。普通桌面回归也改为 `retries: 0`，后续失败直接使 CI 失败。

lint 与 E2E 类型检查通过；托盘生命周期在开发版 **10 次通过（54.5 秒）**、打包版 **10 次通过（50.9 秒）**，均零重试、零跳过。报告为 `test-results/v02-tray-inspector-dev-results.json` 与 `test-results/v02-tray-inspector-packaged-results.json`。后续 CI 复验完整普通回归和 Java 稳定性；产品源码与依赖相对 `41fdaa4` 无变化，已通过的扫描、总览及 10 分钟性能报告保持原提交归属，不冒充新提交执行结果。

托盘修复已推送为 `b3d3d4d3d87eef9909ac141d2ad9046b0db1e23a`。[CI 35100342433](https://github.com/blancmx/CodeHelm/actions/runs/35100342433) 在单元测试阶段失败，尚未进入桌面回归：`startup-protection.test.ts` 中旧 schema 跨启动备份、六次快照轮转两个真实 SQLite 集成用例超过框架默认 5 秒；清理阶段同时出现 EPERM，可能与超时后的未完成操作有关。结果为 696 通过、2 失败，失败日志及证据分别为 `test-results/v02-ci-b3d3d4d-failed.log`、`test-results/v02-ci-b3d3d4d`。这次失败不能记作托盘复验成功。

两个用例分别执行多轮真实备份、文件同步和哈希校验，属于数据完整性测试，没有定义 5 秒产品性能要求。仅为这两个用例显式设置 30 秒执行上限，保留全部断言、真实 SQLite 与磁盘操作，不添加重试；其他用例的默认时限、启动性能采样和桌面性能门槛不变。该调整属于测试预算修正，不证明数据库性能变快。本地 lint 通过，全量 74 个文件、698 项测试通过（27.4 秒），日志为 test-results/v02-db-integration-budget-tests.log；远端零重试复验待完成。

1. 当前提交的 CI 全部结束后，保存报告、原始性能样本和候选哈希；记录失败或重试，不只记录绿色结论。
2. 全部可在当前环境和 CI 完成的验收通过后，再安排 [隔离 Windows 安装矩阵](./09-Windows隔离安装升级验收清单.md)。目录包和 schema fixture 不能代替真实升级、回退或冷启动。
3. 最低支持系统、最终冻结源码与分发方式仍需在安装验收中落实；最终安装器需与被测资产对应。
4. `releaseReady=false` 继续保留。整个 v0.2 不因功能实现或单次 CI 成功而自动标记为发布完成。
