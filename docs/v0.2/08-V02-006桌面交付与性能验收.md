# V02-006 桌面交付与性能验收

> 日期：2026-09-08
> 状态：进行中。已建立本地目录包、迁移、性能及证据采集入口；完整安装升级矩阵与发布验收未关闭。
> 依据：开发规划第 4.6 节和第 9 节。v0.1 的独立门禁不在本阶段重新处理。

## 1. 本轮实现

- 同一 Electron 业务套件支持开发构建和指定的 `CodeHelm.exe`。全部启动和重开均使用所选二进制，并创建独立用户目录。
- 开发版与打包版测试输出分别落在 `e2e-artifacts`、`packaged-e2e-artifacts`，各自生成 JSON 结果，避免互相覆盖截图。
- 从 v0.1.0 已发布提交提取 schema fixture，新增真实 Electron 启动迁移测试，校验项目、方案、设置、历史和迁移前备份。
- 新增显式启用的 10 分钟日志压力场景，约 1,000 行/秒、200 字节/行，每 5 秒采集一次 Electron 应用进程工作集总量，记录停止耗时。
- 新增候选证据脚本：记录 HEAD、分支、未提交状态、源码文件摘要、依赖锁摘要、工具版本、系统信息、逐个产物哈希和测试报告摘要。
- CI 增加 Python/Java fixture 配置、CSC 存在性检查、原生专项测试、隔离打包和打包版业务回归，始终上传测试证据。CI 中缺少必需 fixture 会拒绝运行，不以跳过测试代替覆盖。

代码入口：[desktop-lifecycle.spec.ts](../../e2e/desktop-lifecycle.spec.ts)、[playwright.config.ts](../../playwright.config.ts)、[ci.yml](../../.github/workflows/ci.yml)、[collect-candidate-evidence.mjs](../../apps/desktop/scripts/collect-candidate-evidence.mjs)。

## 2. 本地候选与复现

本轮使用当前工作区构建，HEAD 为 `cd97b6136dd55a979f662b102f5fce9c782e418e`，包含尚未提交的后续阶段实现及既有工作。不是不可变的已发布提交，也没有上传到 GitHub。

目录包位于 `test-results/v02-006-package/win-unpacked`。安装器单独放在 `test-results/v02-006-installer`，保留原 `dist-release`，不在当前电脑执行安装/卸载。

```powershell
pnpm.cmd build
pnpm.cmd --filter @codehelm/desktop exec electron-builder --dir --config.directories.output=../../test-results/v02-006-package
node apps/desktop/scripts/verify-release-artifacts.mjs test-results/v02-006-package/win-unpacked/resources

$env:CODEHELM_E2E_EXECUTABLE = (Resolve-Path 'test-results/v02-006-package/win-unpacked/CodeHelm.exe').Path
$env:CODEHELM_E2E_PYTHON = 'E:\Adeveloppython\python\python-3.31.0\python.exe'
$env:CODEHELM_E2E_JAVA = 'C:\Program Files\Java\jdk-21\bin\java.exe'
$env:CODEHELM_E2E_CSC = 'C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe'
$env:CODEHELM_E2E_PERFORMANCE = '1'
pnpm.cmd test:e2e:built

pnpm.cmd --filter @codehelm/desktop exec electron-builder --prepackaged ../../test-results/v02-006-package/win-unpacked --win nsis --config.directories.output=../../test-results/v02-006-installer
node apps/desktop/scripts/collect-candidate-evidence.mjs
node apps/desktop/scripts/collect-candidate-evidence.mjs test-results/v02-006-installer test-results/v02-006-installer-evidence.json
```

上述运行时路径为本机实际路径，其他机器需替换。普通业务回归可不设置 `CODEHELM_E2E_PERFORMANCE`，此时持续性能场景会明确跳过，不能宣称完成性能门禁。

## 3. 数据升级证据

[v010-schema.sql](../../e2e/fixtures/v010-schema.sql) 来源为 v0.1.0 提交 `11c1a7a8e3bc67b90dee3bd36e4e87fd0ab37b0f` 的数据库 schema；该版本 `user_version=2`，当前为 3。

测试构造 schema 2 的项目、方案、设置和失败历史，在含中文和空格的独立用户目录启动候选。启动后通过真实 IPC 检查关键数据，关闭后检查主库 schema 3，迁移前备份仍为 schema 2 且旧项目可读。

这证明应用层数据迁移和迁移前备份，不等同于安装器版本升级或旧二进制回退。真正的 `0.1 → 0.2` 安装器验收须在冻结版本号后进行；本轮没有修改产品版本，候选仍标为 `0.1.0`。

## 4. 产物核验

| 项目 | 结果 |
|---|---|
| 目录包 ASAR/原生模块/NOTICE 核验 | 通过，66 个 ASAR 条目 |
| Windows 原生边界专项 | 6 项通过 |
| 类型检查、lint | 本轮通过 |
| 本轮安装器大小 | 112,794,540 字节 |
| 本轮安装器 SHA-256 | `4C7C34346FAE0116D2BE78A87E206EEA8232B8FC5105F3E5DE2BB981F7E2535C` |
| Authenticode | `NotSigned`，仅作本地测试候选 |
| 原 v0.1 安装器 SHA-256 | `03E4FA01C45EEBAC2349E2C9569D09D805979713979BF4A88D27A71C5B7EB4CD`，与既有清单一致 |
| 打包版业务与持续性能 | 17 项通过，11.1 分钟；本次无跳过项 |
| 补丁与文档链接 | `git diff --check` 通过；本目录缺失链接 0 项 |

候选证据脚本永远明确输出 `releaseReady: false`，不自动将测试报告转换成发布批准。产物清单排除运行后生成的 `logs` 目录；日志仍保留为测试运行数据，不冒充发行文件。

## 5. 性能口径与未覆盖项

持续日志采样使用 `app.getAppMetrics()` 中各 Electron 进程的工作集之和。它是本机应用进程的内存指标，不包括受管项目进程，也不是跨平台严格 RSS；共享页面可能在多个进程中重复计入。比较相同口径的 120～180 秒与最后一分钟中位数，并记录绝对峰值，阈值为增长不超过 20%。停止耗时从点击运行中心停止按钮开始，直至受管会话数归零，目标不超过 1 秒。

本轮持续测试结果：120 次采样；前稳态中位数 533.83 MiB，最后一分钟中位数 523.38 MiB，变化 **-1.96%**，峰值 **548.05 MiB**；点击停止至会话归零 **924 ms**，两项阈值均通过。测试机为 Windows 构建 `10.0.26200`、AMD Ryzen 9 7940HX、可见物理内存 16,340,414,464 字节；Node 24.16.0、Electron 44.0.0、electron-builder 26.15.3。

原始证据：[性能采样 JSON](../../test-results/packaged-e2e-artifacts/desktop-lifecycle-measures-abfd9-log-memory-and-stop-latency/performance.json)、[停止后的实际窗口](../../test-results/packaged-e2e-artifacts/desktop-lifecycle-measures-abfd9-log-memory-and-stop-latency/performance-stopped.png)、[v0.1 schema 升级窗口](../../test-results/packaged-e2e-artifacts/desktop-lifecycle-migrates-0ea7b-usable-pre-upgrade-snapshot/upgrade-v010.png)。已检查这些实际窗口截图。

完整打包版结果：[17 项测试报告](../../test-results/packaged-e2e-results.json)。候选清单：[目录包](../../test-results/v02-006-evidence.json)、[安装器](../../test-results/v02-006-installer-evidence.json)。这些本地产物需在下一次运行前归档，不自动进入 Git 或公开发布。

本轮是开发工作站测量，期间存在构建/文件校验等其他工作，不能代替专用空闲测试机上的发布基准。目标负载由 fixture 定时器产生，实际吞吐可能受调度和输出背压影响。

### 5.1 千项目搜索、诊断与热启动补测

2026-09-08 后续补测使用同一个隔离目录包和临时 SQLite，写入 1,000 个项目。保留用户当前开发窗口，不改其数据；一次预热及首次 schema 迁移不计入热启动样本。

| 场景 | 样本与测量口径 | 结果 |
|---|---|---|
| 千项目快速搜索 | 30 个不同精确名称；从真实 input 事件至匹配结果可见，再等待两个动画帧 | P95 **19.50 ms**，低于 200 ms |
| 本地 Node 方案诊断 | 30 次真实 IPC；检查返回项非空、没有 blocked，且没有活动会话 | P95 **8.62 ms**，低于 5 秒 |
| 当前候选热启动 | 10 次关闭重开；从启动到总览标题可见且 IPC 返回 1,000 项目 | 中位数 **756.05 ms**，P95 **805.64 ms** |

启动中位数使用偶数样本中间两项的平均值，P95 使用 nearest-rank。搜索样本是在快速搜索弹窗中筛选到单个项目，不代表总览的所有组合筛选或大量匹配结果；诊断只有一个本地 Node 方案，不代表所有语言、磁盘和异常环境。热启动没有清除 OS 缓存，也没有 v0.1 二进制对照，因此不宣称启动回退比例达标。

入口：[独立性能套件](../../e2e/desktop-performance.benchmark.ts)、[独立配置](../../playwright.performance.config.ts)。命令：`pnpm.cmd exec playwright test --config=playwright.performance.config.ts`。默认使用本轮隔离目录包，也可通过 `CODEHELM_E2E_EXECUTABLE` 指定其他测试二进制。结果目录与之前的 17 项回归完全分开。

证据：[原始样本 JSON](../../test-results/v02-006-benchmarks/desktop-performance.benchm-8e0c6-nostics-and-ten-warm-starts/benchmark.json)、[实际千项目搜索窗口](../../test-results/v02-006-benchmarks/desktop-performance.benchm-8e0c6-nostics-and-ten-warm-starts/1000-project-search.png)、[独立测试报告](../../test-results/v02-006-benchmark-results.json)。已查看实际截图，项目数、查询内容及匹配结果可见，未发现遮挡。定向测试通过，补测不改变现有发布状态。

补测脚本已通过 `pnpm.cmd run typecheck`、`pnpm.cmd run lint`；`git diff --check` 无空白错误。临时数据目录清理前校验绝对路径及 fixture 前缀，避免误删其他数据。

### 5.2 剩余性能证据

以下目标仍需独立采集，不由上述样本通过推断：

- 总览组合筛选和大量匹配结果的输入响应；当前已覆盖千项目快速搜索的 30 次精确匹配。
- 正常/深层/权限异常扫描各至少 10 次取消时延，以及配置预算边界。
- Python/Java 与异常环境的诊断 P95；当前已覆盖单个正常 Node 方案的 30 次诊断。
- 同机 v0.1 与候选各 10 次冷启动、10 次热启动；必须区分缓存条件。
- 专用机重复 10 分钟日志试验、实测输出吞吐和输入响应延迟。

## 6. 安装与发布剩余门禁

当前未发现可调用的 Windows Sandbox 入口，未获得可重置虚拟机或独立测试机，因此没有执行安装器对系统注册、快捷方式、卸载和默认用户数据的变更。已准备 [Windows 隔离安装升级验收清单](./09-Windows隔离安装升级验收清单.md)，涵盖全新安装、同版本重装、卸载重装、真实旧版升级、回退、无开发工具环境和异常磁盘/权限。

待关闭事项：

1. 提供隔离 Windows 环境并完成安装矩阵，冻结准确最低支持系统和 x64 架构；本轮仅能确认当前 Windows 构建上的实测结果。
2. 完成上述性能样本与冷/热启动对照。
3. 提交冻结源码，执行远端 Windows CI；本轮仅修改工作流，没有推送或远端 CI 通过证据。
4. 确认版本号、签名与分发渠道，基于冻结提交重建并复验最终资产，随后另行进行公开发布授权。

在这些项目关闭前，V02-006 和整个 v0.2 均不标记为发布验收完成。
